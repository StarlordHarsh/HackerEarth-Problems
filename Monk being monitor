from collections import Counter
for _ in range(int(input())):
    _ = int(input())
    arr = [int(i) for i in input().split()]
    counted = sorted(list(Counter(arr).values()))
    if (counted[-1]-counted[0]):
        print(counted[-1]-counted[0])
        continue
    print(-1)
